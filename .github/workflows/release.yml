name: Release new Version

on:
  pull_request:
    types: [closed]

jobs:
  build:
    name: Build
    runs-on: windows-2022
    if: github.event.pull_request.merged
    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - id: tag
        uses: K-Phoen/semver-release-action@master
        with:
          release_strategy: tag

      - name: Add msbuild to PATH
        uses: microsoft/setup-msbuild@v1
        with:
          vs-version: "17.7"

      - name: Setup NuGet
        uses: nuget/setup-nuget@v1

      - name: Build & Package
        run: ./build.bat ".\SRS-Radio-Presets" ${{ steps.tag.outputs.tag }}

      - name: Setup VSTest
        uses: Malcolmnixon/Setup-VSTest@v3

      - name: VSTest DCS-SR-CommonTests
        run: VSTest.Console DCS-SR-CommonTests\bin\x64\Release\DCS-SR-CommonTests.dll
        env:
          ACTIONS_ALLOW_UNSECURE_COMMANDS: true

      - name: Create a GitHub release
        if: github.ref == 'refs/heads/master'
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ steps.tag.outputs.tag }}
          name: Release ${{ steps.tag.outputs.tag }}
          body: "Please update Release notes"
          artifacts: "Vanguard-SRS-${{ steps.tag.outputs.tag }}.zip"
          token: ${{ secrets.GITHUB_TOKEN }}
          draft: true
